<?php

namespace PubNub\Models\Consumer\DataSync;

/**
 * Response of the single-user DataSync operations.
 *
 * User is a predefined entity class, so the record carries exactly the same fields as a generic entity.
 */
class PNDataSyncUserResult extends PNDataSyncEntityResult
{
}
